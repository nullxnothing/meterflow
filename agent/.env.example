# ═══════════════════════════════════════════════════
# INFINITE Treasury Agent — Environment Config
# ═══════════════════════════════════════════════════

# Solana RPC (Helius recommended for reliability)
HELIUS_RPC_URL=https://mainnet.helius-rpc.com/?api-key=YOUR_HELIUS_KEY

# The treasury wallet that receives 50% of creator fees
TREASURY_WALLET=YOUR_TREASURY_WALLET_PUBLIC_KEY

# API proxy server (for pushing rate limit updates)
PROXY_ADMIN_URL=http://localhost:3001
PROXY_ADMIN_KEY=your-admin-secret

# Cost assumptions
COST_PER_CALL_USD=0.02
# SOL_PRICE_USD is auto-fetched from Jupiter, but set a fallback
SOL_PRICE_USD=150

# Agent health server port
AGENT_PORT=3002
